# config/settings.py

# Configuration settings for the generative model and resume builder
MODEL_VERSION = "v1.0"  # Replace with the desired version
MODEL_CONFIG = {
    "max_tokens": 500,
    "temperature": 0.7,
    "top_p": 0.9,
    # Add more config parameters as required by your model
}

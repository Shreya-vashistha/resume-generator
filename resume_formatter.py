# utils/resume_formatter.py

def format_resume(text):
    """
    Further format the resume text if needed (e.g., applying Markdown formatting).
    This function can be extended based on specific formatting requirements.
    """
    # For now, simply return the text.
    return text
